package dev.jpa.allimio.member.updatelog;

import org.springframework.data.jpa.repository.JpaRepository;

public interface MemberUpdatelogRepository extends JpaRepository<MemberUpdatelog, Long> {
  
}
